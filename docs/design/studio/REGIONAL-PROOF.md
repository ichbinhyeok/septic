# Regional entry to research value

Studio state and county previews now surface a photographic research case early in the main content. County overview and direct official-route anchors remain intact. The county page index includes Our research; later file-section links point to the selected modern case rather than the legacy brief page.

Selection is explicit: matching county, then same-state example, otherwise Haverhill's cross-source investigation. Every instance prints the case's actual geography and states that it illustrates the method rather than local record availability. Never infer a property's outcome from the selected case. No private operational data is loaded at runtime.

Journey verified by actual GET-only clicks: Autauga county preview -> Morgan investigation -> intake. No intake submission. Desktop 1440 and mobile 390 checked; Georgia fallback geography checked; no overflow or console errors. 28 preview/intake/disabled-preview tests passed, including early placement and selector checks.

Existing generated images reused as atmosphere. No production pSEO routes or SEO metadata modified; this remains opt-in preview only. Conversion uplift is unmeasured, not claimed.
