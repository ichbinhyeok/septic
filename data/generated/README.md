# Generated Data

This directory is reserved for derived application data.

Examples:

- `state_pages.json`
- `calculator_rules.json`
- `source_sets.json`

V1 can load directly from `data/raw/`, so this directory may stay minimal until
generation becomes useful.
